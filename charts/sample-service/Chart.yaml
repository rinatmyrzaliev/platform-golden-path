apiVersion: v2
name: sample-service
description: A minimal consumer chart that uses the platform-library chart.
type: application
version: 0.1.0
appVersion: "1.0.0"

dependencies:
  - name: platform-library
    version: 1.0.0
    repository: "file://../platform-library"
